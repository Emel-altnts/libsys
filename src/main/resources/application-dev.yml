spring:
  kafka:
    group-id: project-api
    bootstrap-servers: localhost:19081


    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: 1  # 'all' yerine '1' kullanın (tek broker için)
      retries: 3
      retry-backoff-ms: 1000
      request-timeout-ms: 30000
      delivery-timeout-ms: 120000

    consumer:
      group-id: libsys-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      enable-auto-commit: false
      session-timeout-ms: 30000
      heartbeat-interval-ms: 3000
      max-poll-interval-ms: 300000
      fetch-min-size: 1
      fetch-max-wait: 500
      properties:
        spring.json.trusted.packages: "com.d_tech.libsys.dto"
        spring.json.value.default.type: "com.d_tech.libsys.dto.UserRegistrationEvent"

    listener:
      ack-mode: manual_immediate
      concurrency: 1  # İlk teste için 1 yapın
      poll-timeout: 3000
      missing-topics-fatal: false  # Topic yoksa hata vermesin